Q1_sort_string.cpp
Contains:
  sort_string(): returns a new sorted string
  sort_string_inplace(): sorts inside the same string
  Case-insensitive comparison
  Selection sort
  Supports sorting first n characters

Q2_validator.cpp
Validates:
  ZIP filename format: lab<labNumber>_<ERP>.zip
  Task file names: task<number>.cpp
  No spaces or special characters
  Optional verbose mode

  ERP used: 25985

  Test cases:
  Valid submission
  Invalid ZIP name
  Invalid task number
  Special character in filename

Q3_calculator.cpp
Features:
  Operators: +, -, *, /
  Commands: help, history, quit
  Division by zero handling
  Uses vector<int[4]> for history
  Switch statement for arithmetic

Q4_maze.cpp
  Recursive maze solver that:
  Finds path from S to E
  Uses DFS recursion
  Marks visited cells
  Backtracks correctly

Prints final maze and coordinates
# Assignment_3
